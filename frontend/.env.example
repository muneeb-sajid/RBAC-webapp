# Base URL for your Node.js/Express backend API.
# During local development against the mock data layer, this value is unused
# unless you set USE_MOCKS to false in src/services/api.js.
VITE_API_URL=http://localhost:4000/api
